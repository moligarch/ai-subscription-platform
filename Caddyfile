# Admin Panel API
admin.localhost {
    # Forward requests to our Go application container on port 8080
    reverse_proxy telegram-ai:8080
}

# Grafana Dashboard
grafana.localhost {
    # Forward requests to our Grafana container on port 3000
    reverse_proxy grafana:3000
}