{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "envFile": "${workspaceFolder}/.env",
            // "program": "${fileDirname}",
            "program": "${workspaceFolder}/cmd/app",
            "args": ["--config", "${workspaceFolder}/config.yaml"],
        }
    ]
}